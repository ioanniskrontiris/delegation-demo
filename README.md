# Delegation Demo (Agent ↔ Gateway ↔ Tool)
## Start servers
npm install
npm run dev
## Start agent
cd agent && python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python main.py